{
  "name": "ai-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check src/simulation.mjs && node --check src/index.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
